---
phase: 06-inline-fix
plan: 03
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - public/styles.css
autonomous: true
requirements: [FIX-01, FIX-02]
must_haves:
  truths:
    - "public/styles.css contains a `.grocery-pencil` rule with width:24px, height:24px (matches .grocery-delete shape per D-68)."
    - "public/styles.css contains a `.recipe-pencil` rule with width:20px, height:20px (smaller for recipe-page text rhythm per UI-SPEC line 143)."
    - "Both `.grocery-pencil:hover` and `.recipe-pencil:hover` set color to var(--accent) (orange — distinct from .grocery-delete:hover which uses var(--error))."
    - "`.library-fix-editor` and `.library-categorize-editor` containers have padding 12px 14px and background var(--card-bg) matching the spacing scale (UI-SPEC § Spacing Scale)."
    - "`.library-fix-fields` / `.library-categorize-fields` use `display:flex; flex-wrap:wrap; gap:8px` and stack on `(max-width: 480px)` (responsive contract)."
    - "`.library-fix-save` / `.library-categorize-save` mirror `.library-add button` orange-pill styles (var(--accent) bg, white text, var(--radius) corners) per UI-SPEC § Color."
    - "`.library-categorize-error` color is var(--error) (#b54632) per UI-SPEC § Color destructive-reservation."
    - "`.recipe-ingredient-line` uses `display:flex; align-items:center; gap:8px` so the pencil floats to the right of the text."
  artifacts:
    - path: "public/styles.css"
      provides: "~140 new lines appended to existing /* Library */ block per UI-SPEC § CSS Additions Summary"
      contains: ".grocery-pencil"
  key_links:
    - from: "public/styles.css .grocery-pencil:focus-visible"
      to: "WCAG keyboard navigation"
      via: "outline:2px solid var(--accent); outline-offset:1px"
      pattern: ":focus-visible \\{[^}]*outline:"
---

<objective>
Wave 2: append CSS for the pencil button and the two new inline editors.

This plan runs in parallel with Plan 02 (no file overlap — Plan 02 modifies routes/library.js + new view partials + new test file; Plan 03 modifies only public/styles.css). Both depend on Plan 01's templates being in place so the new CSS rules apply to existing markup.

The CSS contract is fully specified in `06-UI-SPEC.md` § CSS Additions Summary (lines 305-449) — copy verbatim into `public/styles.css` AFTER the existing `/* Library */` block (after line 496). All new classes prefixed `library-fix-*` / `library-categorize-*` / `.grocery-pencil` / `.recipe-pencil` / `.recipe-ingredient-line` per CONTEXT.md "Claude's Discretion — CSS class naming". No new design tokens; reuses `--accent`, `--muted`, `--error`, `--bg`, `--card-bg`, `--border`, `--fg`, `--radius`.

Purpose: Without this CSS, the pencil button has no visual styling (default browser button + black text), the editors render as wall-of-form fields, and the recipe-ingredient-line does not flex (text + pencil would render top-to-bottom rather than left-to-right). All visual quality from D-68 (icon-only same-size-as-delete) and UI-SPEC's design contract depends on this plan landing.

Output: ~140 new lines in `public/styles.css`. Zero new variables, zero new media queries (one `@media (max-width: 480px)` block for the editor stack — UI-SPEC line 444).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/06-inline-fix/06-UI-SPEC.md
@.planning/phases/06-inline-fix/06-PATTERNS.md
@.planning/phases/06-inline-fix/06-CONTEXT.md
@public/styles.css

<interfaces>
<!-- CSS variables already defined in :root (line 1-11 of public/styles.css) -->
<!-- Phase 6 reuses these verbatim; do NOT introduce new variables. -->

From public/styles.css :root:
- `--bg #fafaf7` — page background (60%)
- `--card-bg #ffffff` — card surface (30%)
- `--border #e5e2da` — separator
- `--accent #c45a3a` — primary action / orange (10%)
- `--error #b54632` — destructive
- `--muted #5a5a5a` — secondary text
- `--fg #2b2b2b` — body text
- `--radius` — border-radius token (verify exact value in :root)

Existing analog rules (lines mentioned per PATTERNS.md):
- `.grocery-delete` (lines 314-323) — icon button shape, 24x24, hover var(--error)
- `.library-add button` (lines 436-444) — orange pill, padding 10px 18px, var(--accent)
- `.library-row` (lines 476-484) — flex row container, padding 8px 4px, border-bottom
- `@media (max-width: 640px)` block at line 413 — existing breakpoint precedent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append CSS additions to public/styles.css</name>
  <files>public/styles.css</files>
  <read_first>
    - public/styles.css (read tail of file — lines 470-496 to find the end of the existing /* Library */ block)
    - .planning/phases/06-inline-fix/06-UI-SPEC.md (lines 305-449 — CSS Additions Summary; copy verbatim)
    - .planning/phases/06-inline-fix/06-PATTERNS.md (Section 7 — analog references and what to copy from existing rules)
  </read_first>
  <action>
    Append the following CSS block to the end of `public/styles.css` (after the last `}` of the existing `/* Library */` block — verify by reading the tail of the file first to confirm the insertion point). The block is verbatim from UI-SPEC.md § CSS Additions Summary lines 305-449:

    ```css
    /* Phase 6: Inline Fix / Categorize */

    /* Pencil affordance — slots into existing grocery + recipe rows */
    .grocery-pencil,
    .recipe-pencil {
      width: 24px; height: 24px;
      border: none;
      background: transparent;
      color: var(--muted);
      cursor: pointer;
      padding: 0;
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }
    .recipe-pencil { width: 20px; height: 20px; }
    .grocery-pencil:hover,
    .recipe-pencil:hover { color: var(--accent); }
    .grocery-pencil:focus-visible,
    .recipe-pencil:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 1px;
      border-radius: 3px;
      color: var(--accent);
    }

    /* Recipe ingredient line gains flex layout */
    .recipe-ingredient-line {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .recipe-ingredient-line .recipe-ingredient-text { flex: 1; }

    /* Inline editor containers (replace the row's <li> via outerHTML) */
    .library-fix-editor,
    .library-categorize-editor {
      display: block;
      padding: 12px 14px;
      background: var(--card-bg);
      border-bottom: 1px solid var(--border);
    }
    .library-fix-editor:last-child,
    .library-categorize-editor:last-child { border-bottom: none; }

    .library-fix-header,
    .library-categorize-header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 8px;
      margin-bottom: 12px;
      font-size: 13px;
      color: var(--muted);
    }
    .library-fix-header strong { color: var(--fg); font-weight: 500; }

    .library-fix-form,
    .library-categorize-form {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    .library-fix-fields,
    .library-categorize-fields {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }
    .library-fix-field,
    .library-categorize-field {
      display: flex;
      flex-direction: column;
      gap: 4px;
      min-width: 140px;
      flex: 1;
    }
    .library-fix-field label,
    .library-categorize-field label {
      font-size: 13px;
      color: var(--muted);
    }
    .library-categorize-field input[type="text"] {
      padding: 8px 10px;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      font-size: 16px;
      background: var(--card-bg);
    }
    .library-fix-field select,
    .library-categorize-field select {
      padding: 8px 10px;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      font-size: 14px;
      background: var(--card-bg);
    }

    .library-categorize-error {
      font-size: 12px;
      color: var(--error);
      line-height: 1.4;
    }

    .library-fix-actions,
    .library-categorize-actions {
      display: flex;
      gap: 8px;
      margin-top: 4px;
    }
    .library-fix-save,
    .library-categorize-save {
      padding: 8px 16px;
      border: none;
      background: var(--accent);
      color: white;
      border-radius: var(--radius);
      font-size: 14px;
      cursor: pointer;
    }
    .library-fix-cancel,
    .library-categorize-cancel {
      padding: 8px 16px;
      border: 1px solid var(--border);
      background: transparent;
      color: var(--muted);
      border-radius: var(--radius);
      font-size: 14px;
      cursor: pointer;
    }
    .library-fix-cancel:hover,
    .library-categorize-cancel:hover { color: var(--fg); border-color: var(--fg); }

    .library-fix-link {
      font-size: 13px;
      color: var(--accent);
    }

    @media (max-width: 480px) {
      .library-fix-fields,
      .library-categorize-fields { flex-direction: column; }
      .library-fix-field,
      .library-categorize-field { min-width: 0; }
    }
    ```

    Use the Edit tool's "append" pattern — read the tail of the file, then write the file with the new block appended after the last existing rule. DO NOT modify any existing rules (especially `.grocery-delete`, `.library-add`, `.library-row`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('public/styles.css','utf8');const checks=[/\.grocery-pencil[\s,]/,/\.recipe-pencil[\s,]/,/\.grocery-pencil:hover[\s,]/,/\.recipe-pencil:hover[\s,]/,/\.recipe-ingredient-line\s*\{/,/\.library-fix-editor[\s,]/,/\.library-categorize-editor[\s,]/,/\.library-fix-header[\s,]/,/\.library-categorize-header[\s,]/,/\.library-fix-form[\s,]/,/\.library-categorize-form[\s,]/,/\.library-fix-fields[\s,]/,/\.library-categorize-fields[\s,]/,/\.library-fix-save[\s,]/,/\.library-categorize-save[\s,]/,/\.library-fix-cancel[\s,]/,/\.library-categorize-cancel[\s,]/,/\.library-categorize-error\s*\{/,/\.library-fix-link\s*\{/,/@media \(max-width: 480px\)/];for(const r of checks){if(!r.test(c)){console.error('MISSING:',r.toString());process.exit(1);}}console.log('OK')"</automated>
  </verify>
  <done>
    - `public/styles.css` contains all 20 new selectors checked above.
    - No existing rules removed or modified (run `npm test` to confirm no test inspecting CSS-substring assertions broke — there are none expected to depend on CSS, but verify).
    - The `.grocery-pencil` width/height is 24px (matches `.grocery-delete`) and `.recipe-pencil` is 20px (smaller per UI-SPEC).
    - `@media (max-width: 480px)` block exists with `flex-direction: column` rules for the editor fields.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies a static asset (CSS only — no JavaScript, no markup, no routes). No new trust boundary introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | n/a | public/styles.css (static asset) | accept | No new threats. CSS cannot execute scripts (Express serves CSS with `Content-Type: text/css`); no third-party fonts, images, or `@import` rules added. UI-SPEC.md verified the additions use only existing CSS variables — no inline `url(...)` or external resources introduced. |
</threat_model>

<verification>
After Plan 03 lands:
- `npm test` exits 0 (CSS doesn't affect test outcomes; this is a sanity check that no other plan was accidentally touched).
- Manual smoke test: open `/grocery` and confirm the pencil button renders as a 24x24 transparent button with muted-color icon. (Manual; not automated per VALIDATION.md "Manual-Only Verifications" table — visual fidelity is browser-dependent.)
- `grep -c '\.grocery-pencil' public/styles.css` ≥ 3 (selector appears in main rule + hover + focus-visible).
</verification>

<success_criteria>
1. `.grocery-pencil` and `.recipe-pencil` rules exist with the icon-button shape from UI-SPEC § CSS (D-68).
2. Hover state uses `var(--accent)` (orange — distinct from delete-button red).
3. Focus-visible state has 2px accent outline (WCAG keyboard navigation).
4. Editor containers (`.library-fix-editor`, `.library-categorize-editor`) match `.grocery-item` padding scale.
5. Save buttons (`.library-fix-save`, `.library-categorize-save`) mirror `.library-add button` orange-pill styles.
6. `.library-categorize-error` uses `var(--error)` per UI-SPEC § Color destructive-reservation.
7. `@media (max-width: 480px)` block stacks editor fields vertically.
8. Zero new CSS variables introduced; all rules reference existing :root tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/06-inline-fix/06-03-SUMMARY.md`.
</output>
